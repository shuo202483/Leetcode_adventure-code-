这是通过两个栈来模拟队列的一种方法

栈出入顺序都是在栈顶进行操作（时间复杂为O（1））
每个元素最多被 push 一次、从 in_stack pop 一次、push 到 out_stack 一次、从 out_stack pop 一次——共 4 次常数时间操作

但是无论如何操作，队列里依旧有n个元素（它们都在内存当值），所以空间复杂度为O(N)

最后我这边讲一下在调用这个类的时候所用到的一些函数的意思
zip:用于把同一个位置（下标）的两个（或多个）列表一对一配对
getattr(obj, op)(*arg）：(对象（库、类、字符串等等）,对象支持的操作)，(*arg) 表示把参数列表拆开（拆包）来调用，因为在这个案例中每个值都是一个小列表。

 if op == "MyQueue":
        obj = MyQueue()
        res.append(None)     
当第一次遍历的时候我们会进行初始化生成一个MyQueue的类


